```
{
    "display_limit": 10,       // 画面に一度に表示する件数(実装中)
    "chunk_size": 10,          // linksに保存したrss毎に記事を取得する件数
    "bookmarks": [
      "https://example1.com",  // ブックマークした記事
      ...
    ],
    "links": [
      "https://exsample1.rss",  // RSSのURL
      "https://exsample2.rss",
      ...
    ],
  "history_expiration": 90    // 履歴の保存期間(実装中)
}
```
